# ==========================================================
# Study Group Finder - Environment Configuration
# ==========================================================

# Server Port
PORT=4000

# MongoDB Database Connection String
MONGODB_URI=mongodb://127.0.0.1:27017/study_group_finder

# JWT Authentication Secret & Expiry
JWT_SECRET=study_group_finder_super_secret_jwt_key_academic_elegance_2026
JWT_EXPIRES_IN=7d

# Frontend Base API URL (used by React client)
VITE_API_URL=http://localhost:4000/api
